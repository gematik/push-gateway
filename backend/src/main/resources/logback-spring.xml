<?xml version="1.0" encoding="UTF-8" ?>
<configuration>

    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{ISO8601} [%blue(%t)] %highlight(%-5level) %green(%logger{10}) %yellow([%file:%line]) %msg %red(%mdc%n)</pattern>
        </encoder>
    </appender>

    <root level="${LOG_LEVEL:-INFO}">
        <appender-ref ref="Console"/>
    </root>
</configuration>

{{ $fullName := include "push-gateway.fullname" . }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullName }}-consumer
  labels:
    {{- include "push-gateway.consumer.labels" . | nindent 4 }}
    app.kubernetes.io/app: {{ $fullName }}-consumer
  {{- if eq (.Values.service.useGatewayAPI) false }}
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
    cloud.google.com/app-protocols: '{"api": "HTTP","management": "HTTP"}'
    cloud.google.com/backend-config: '{"default": "erezeptapp-pushgw-backendconfig"}'
  {{- end }}
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: api
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: management
{{ if .Values.backend.config.debug }}
    - port: 5005
      targetPort: 5005
      protocol: TCP
      name: debug
{{ end }}
  selector:
    {{- include "push-gateway.consumer.selectorLabels" . | nindent 4 }}
